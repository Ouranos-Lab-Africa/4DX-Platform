// This will reset the rate limit by restarting the process
// Since the rate limit is in-memory, we need to restart the backend server

console.log("To reset rate limit, the backend server needs to be restarted.");
console.log("The rate limit will also naturally reset after 15 minutes.");
console.log("");
console.log("To proceed with testing, we'll:");
console.log("1. Restart the backend server");
console.log("2. Then try logging in again");
